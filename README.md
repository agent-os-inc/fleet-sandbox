# Updated README after adding the `add_numbers` function and its tests
Throwaway target repo for AgentOS fleet build→PR proof (safe to delete)

## New Features

- `add_numbers(a, b)`: Returns the sum of two numbers. Validated with tests in `test_calc.py`.